const BLUE = '#040b27';
const WHITE = 'white';
const LIGHT_BLUE = '#4789c0';

const theme = {
    dark: {
        background: {
            primary: BLUE,
            secondary: WHITE,
        },
        color: {
            primary: WHITE,
            secondary: BLUE,
            border: LIGHT_BLUE,
        },
    },
    light: {
        background: {
            primary: WHITE,
            secondary: BLUE,
        },
        color: {
            primary: BLUE,
            secondary: WHITE,
            border: LIGHT_BLUE,
        },
    },
};

export default theme;
