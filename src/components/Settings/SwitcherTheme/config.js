import { THEMES } from '@styled/theme/config';

const { DARK, LIGHT, COLORED } = THEMES;

const OPTIONS = [
    {
        id: 1,
        value: DARK,
    },
    {
        id: 2,
        value: LIGHT,
    },
    {
        id: 3,
        value: COLORED,
    },
];

export default OPTIONS;
